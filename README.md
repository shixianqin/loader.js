# loader.js
just loader

```javascript

var loding = new Loader([radius|option]);
//new Loader(50);
//new Loader({radius: 30, color: "orange", borderWidth: 5});

loading.follow(someElement);    //跟随某个元素加载的方法
loading.show();                 //显示
loading.hide();                 //隐藏

```
